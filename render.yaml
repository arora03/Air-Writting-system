services:
  - type: web
    name: airscript-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.main
    envVars:
      - key: HOST
        value: 0.0.0.0
